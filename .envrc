# The Gradle wrapper is committed and build.gradle declares a Java 17 toolchain that
# Gradle downloads on demand, so a checkout needs no pinned JDK or gradle install.
# Any JDK 17+ on PATH is enough to launch ./gradlew.
