// Lets Gradle download the JDK 17 toolchain itself, so a fresh checkout needs
// nothing installed but the wrapper.
plugins {
    id 'org.gradle.toolchains.foojay-resolver-convention' version '1.0.0'
}

rootProject.name = 'RustyDagger'
